package snowflake;

import java.util.List;

public class TaskMaster {
  //先拓扑排序出可以直接做的task，剩下的找出所有cycle，每个cycle可以做n - 1个
  public int task(int n, List<Integer> a, List<Integer> b) {
    return 0;
  }
}
